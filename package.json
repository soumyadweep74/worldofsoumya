{
  "name": "worldofsoumya-ai-portfolio",
  "version": "1.0.0",
  "description": "Dependencies for Netlify Function.",
  "dependencies": {
    "@google/genai": "^0.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
